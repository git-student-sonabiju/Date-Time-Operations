version: '3.8'
services:
  postgres:
    image: postgres:latest
    container_name: dateTime-postgres
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: date-time
    ports:
      - "5432:5432"